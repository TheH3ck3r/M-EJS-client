import { FC } from "react";
import "./Progress.scss";
export type colorVariants = "primary" | "secondary" | "accent";
export interface IProgressProps {
    /**
     * Значение прогресса
     */
    value: number;
    /**
     * Цвет прогресса
     */
    color: colorVariants | string;
    /**
     * Индикатор полосатости :D
     */
    striped?: boolean;
    /**
     * Анимация индикатора выполнения работает только в сочетании со свойством `striped`.
     */
    animated?: boolean;
}
/**
 * Компонент, который указывает на ход выполнения операции.
 */
declare const Progress: FC<IProgressProps>;
export default Progress;
