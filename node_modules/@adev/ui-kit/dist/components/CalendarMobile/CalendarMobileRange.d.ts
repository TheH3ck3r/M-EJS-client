import { FC } from "react";
import "./CalendarMobileRange.scss";
export interface CalendarMobileRange {
    onChange?: (date: Date[]) => void;
    onClose: () => void;
    disabledWeekDays?: number[];
    disabledDates?: Date[];
    classNameToday?: string;
    defaultValue?: Date[];
}
declare const CalendarMobileRange: FC<CalendarMobileRange>;
export default CalendarMobileRange;
