import React, { FC } from "react";
import "./Switch.scss";
import { defaultProps } from "../../utils/defaultProps";
export type SwitchProps = defaultProps & {
    /**
     * Цвет переключателя (primary, secondary, accent или hex-код)
     */
    color?: "primary" | "secondary" | "accent" | string;
    /**
     * Имя переключателя в DOM-дереве
     */
    name?: string;
    /**
     * Обработчик события изменения состояния переключателя
     * @param event Событие изменения состояния переключателя
     */
    onChange?: (event?: React.ChangeEvent) => void;
    /**
     * Текст, отображаемый при включенном состоянии переключателя
     */
    textOn?: React.ReactNode;
    /**
     * Текст, отображаемый при выключенном состоянии переключателя
     */
    textOff?: React.ReactNode;
    /**
     * Состояние переключателя (включен/выключен)
     */
    checked: boolean;
    /**
     * Выключение переключателя (неактивное состояние)
     */
    disabled?: boolean;
    /**
     * Идентификатор переключателя в DOM-дереве
     */
    inputHtmlId?: string;
};
declare const Switch: FC<SwitchProps>;
export default Switch;
