import React, { FC } from "react";
import "./NotificationBox.scss";
export type INotificationBoxProps = {
    /**
     * Позиция блока уведомлений
     */
    position?: "top-left" | "top-center" | "top-right" | "bottom-left" | "bottom-center" | "bottom-right";
    /**
     * Стили для корневого элемента
     */
    rootStyle?: React.CSSProperties;
    /**
     * Класс для корневого элемента
     */
    rootClassName?: string;
    /**
     * Задержка перед закрытием уведомления
     */
    autoCloseDelay?: number;
};
/**
 * Блок уведомлений, используется для отображения пользовательских уведомлений
 */
declare const NotificationBox: FC<INotificationBoxProps>;
export default NotificationBox;
