import "./InputCard.scss";
export interface CardDetails {
    number: string;
    expireAt: string;
    cvc: string;
}
export interface IInputCardProps {
    /**
     * Определяет значение инпута по умолчанию
     */
    defaultValue: CardDetails;
    /**
     * Обработчик изменения значения инпута
     * @param card - объект с данными карты (посмотрите пример CardDetails в defaultValue в исходном коде)
     */
    onChange: (card: CardDetails) => void;
}
/**
 * Компонент для ввода номера карты
 */
declare const InputCard: ({ defaultValue, onChange }: IInputCardProps) => JSX.Element;
export default InputCard;
