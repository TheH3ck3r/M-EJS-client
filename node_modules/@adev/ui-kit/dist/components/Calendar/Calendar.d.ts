import { FC } from "react";
import "./Calendar.scss";
export interface CalendarProps {
    /**
     * Дата по умолчанию
     */
    defaultValue?: Array<Date>;
    /**
     * Обработчик изменения даты
     * @param value - выбранная дата
     */
    onChange?: (value: Array<Date>) => void;
    /**
     * Даты которые нельзя выбрать
     */
    disabledDates?: Array<Date>;
    /**
     * Дни недели которые нельзя выбрать
     */
    disabledWeekDays?: Array<number>;
    /**
     * Множественный выбор дат
     */
    multiple?: boolean;
    /**
     * Класс для дня
     */
    classNameDay?: string;
    /**
     * Видимость заголовка
     */
    isHeaderVisible?: boolean;
    /**
     * Класс для текущего дня (сегодня)
     */
    classNameToday?: string;
}
export interface CalendarBrowserProps extends CalendarProps {
    /**
     * Месяц, который будет показан в календаре
     * Если не передан, то будет показан текущий месяц
     */
    currentMonth?: number;
    /**
     * Год, который будет показан в календаре
     * Если не передан, то будет показан текущий год
     */
    currentYear?: number;
}
export declare const Calendar: FC<CalendarBrowserProps>;
export default Calendar;
